"use strict"; //use all js code as newer version

// alert(3+3) // it will not work as that of html , because we are using node js not browser..

/*

number => 2 to power53;
strings => "";
boolean => true/false;
bigint => used for storing bigger values , in websites such as stock market etc..
undefined => variable declared , but value is not assigned..    (typeof udefined is undefined)
null => representation of the empty value..                 (typeof null is object )

*/

console.log(typeof undefined);
